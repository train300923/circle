<h1>Here is our list of recommended providers</h1>
  <ul>
    <% @activities_category.each do |activity|%>
      <li>
        <%= cl_image_tag activity.photo.key, height: 300, width: 400, crop: :fill %>
        <%= link_to activity.name, activity_path(activity) %>
      </li>
    <% end %>
  </ul>
