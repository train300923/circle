<h3 class="comment-memories">Girls night out in Vienna, 1st of March. So much fun!!! 💃 🪩 🥳 🍾</h3>
  <div data-controller="memories" class="grid-memories" >
    <% if @memory.photos.attached? %>
      <% @memory.photos.each do |photo| %>
        <div class="photo-container-memory">
          <%= cl_image_tag photo.key, data: { controller: "memories", memories_target: "photo", action: "click->memories#pop" } %>
      </div>
      <% end %>
    <% else %>
    <%= "You haven't posted any photo yet" %>
    <% end %>
  </div>
<%# data-memories-target="photo" data-action="click->memories#pop" %>
